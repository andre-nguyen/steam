- deps:
  - cd src/deps/catkin
  - build:
    - catkin config --extend=/opt/ros/install
    - catkin build

- steam:
  - cd src
  - build:
    - catkin build
  - test:
    - cd ../devel/steam/lib/steam
    - ./steam_unit_tests
