- deps:
  - cd src/deps/catkin
  - build:
    - catkin config --extend=/opt/ros/install
    - catkin build catkin_optional
    - catkin build catch
    - catkin build lgmath

- steam:
  - cd src
  - build:
    - catkin build
  - test:
    - cd ../devel/repo/lib/steam
    - ./steam_unit_tests
